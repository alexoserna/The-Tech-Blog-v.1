<div class="flex-row justify-center ">
    <div class="col-sm-12  col-md-10 col-lg-8 col-xl-6 card info hidden">
        <div class="card-header">
            <h2 id="post_id">{{post.post_id}}</h2>
        </div>
        <div class="card-body col-8">
            <form>
                <div class="input-card p-2">
                    <label class="form-label pl-2">Comment</label>
                    <textarea name="comment-body" rows="2" type="text" class="form-textarea"
                        id="post-content"></textarea>
                </div>
                <button class="btn" id="new-comment-form">Comment</button>
            </form>
        </div>
    </div>
</div>

<div class="flex-row justify-center">
    <button class="btn" id="toggle-btn">➕ New Comment</button>
</div>

<script src="/js/comment.js"></script>